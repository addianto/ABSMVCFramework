module Model.User;
export *;

interface User
{
	Int getId();
	String getUsername();
	String getPassword();
}

class UserImpl(Int id, String username, String password)
	implements User
{	

	Int getId() { return id; }
	String getUsername() { return username; }
	String getPassword() { return password; }
}